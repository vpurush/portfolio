var React = require('react');

require('./header.scss');

class Header extends React.Component{
    render(){
        return (
            <header></header>
        );
    }
}

module.exports = Header;
var React = require('react');

require('./footer.scss');

class Footer extends React.Component{
    render(){
        return (
            <footer></footer>
        );
    }
}

module.exports = Footer;